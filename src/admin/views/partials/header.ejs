<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title><%= title %> - ניהול הזמנות</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<header class="topbar">
  <div>
    <a href="/admin/reservations">הזמנות</a>
    <a href="/admin/customers">לקוחות</a>
    <a href="/admin/locations">מקומות</a>
    <a href="/admin/holidays">חגים</a>
    <% if (typeof currentRole !== 'undefined' && currentRole === 'manager') { %><a href="/admin/users">משתמשים</a><% } %>
  </div>
  <div style="display:flex; align-items:center; gap:12px;">
    <span>שלום, <%= typeof currentUsername !== 'undefined' ? currentUsername : '' %></span>
    <form method="post" action="/admin/logout" class="inline">
      <button type="submit">התנתק</button>
    </form>
  </div>
</header>
<main>
